fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.60
sqlalchemy>=2.0
pypdf>=4
python-multipart>=0.0.9
email-validator>=2
httpx>=0.27

# opcional: retriever semântico (DIREITO_ABERTO_RETRIEVER=semantico)
# chromadb>=1.0

# opcional: PostgreSQL (DIREITO_ABERTO_DATABASE_URL=postgresql+psycopg://...)
# psycopg[binary]>=3

# desenvolvimento / testes
pytest>=8
