fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.60
sqlalchemy>=2.0
pypdf>=4
python-multipart>=0.0.9
email-validator>=2
httpx>=0.27

# opcional: retriever semântico (DIREITO_ABERTO_RETRIEVER=semantico)
# chromadb>=1.0

# PostgreSQL (usado quando DIREITO_ABERTO_DATABASE_URL aponta para Postgres;
# sem a variável, o app usa SQLite e este driver fica ocioso)
psycopg[binary]>=3

# desenvolvimento / testes
pytest>=8
